#include<iostream>
using namespace std;
int main232345324() {

	//空指针:指针变量指向内存中编号为0的空间,用于初始化指针变量,空指针指向的内存是不可访问的;
	//系统编号为0~255的空间为系统占内存,不允许用户访问;

	int* p = NULL;
	cout << p << endl;
	/*p = 100;*///引发了未经处理的异常:写入访问权限冲突.

	//野指针:指针变量指向非法的内存空间;
	int* a = (int*)0x0110;
	cout << *a << endl;///引发了未经处理的异常:写入访问权限冲突.,在程序中尽量避免野指针


	return 0;
}